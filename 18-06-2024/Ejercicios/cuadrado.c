#include <stdio.h>
int main(){
    {
        float base;
        float total;
        printf("ingrese la base del cuadrado: ");
        scanf("%f",&base);
        total=base*base;
        printf("el area del cuadrado es: %f \n",total);
        return 0;
    }
}